{#{% if languages|length > 1 %}
  <div class="dropdown">
    <div class="dropdown-toggle" data-toggle="dropdown">
      {% for language in languages %}
        {% if language.code == code %}
          <img src="catalog/language/{{ language.code }}/{{ language.code }}.png" alt="{{ language.name }}" title="{{ language.name }}">
        {% endif %}
      {% endfor %}
      <span class="d-none d-md-inline">{{ text_language }}</span> <i class="fas fa-caret-down"></i>
    </div>
    <div class="dropdown-menu">
      {% for language in languages %}
        <a href="{{ language.href }}" class="dropdown-item"><img src="catalog/language/{{ language.code }}/{{ language.code }}.png" alt="{{ language.name }}" title="{{ language.name }}"/> {{ language.name }}</a>
      {% endfor %}
    </div>
  </div>
{% endif %}#}

{% if languages|length > 1 %}
    <div id="lang-switcher">
        <div class="dropdown">
            <a aria-expanded="false" aria-haspopup="true" class="dropdown-toggle"
               data-toggle="dropdown" href="#" role="button">
                Ro
            </a>
            <div class="dropdown-menu">
                <a class="dropdown-item active" href="#">Română</a>
                <a class="dropdown-item" href="#">Русский</a>
            </div>
        </div>
    </div>
{#        <form action="{{ action }}" method="post" enctype="multipart/form-data" id="lang-switcher">#}
{#        <div class="dropdown">#}
{#            {% for language in languages %}#}
{#                {% if language.code == code %}#}
{#                    <a aria-expanded="false" aria-haspopup="true" class="dropdown-toggle"#}
{#                       data-toggle="dropdown"#}
{#                       href="#"#}
{#                       role="button">#}
{#                        {{ language.name|slice(0, 2) }}#}
{#                    </a>#}
{#                {% endif %}#}
{#            {% endfor %}#}
{#            <div class="dropdown-menu">#}
{#                {% for language in languages %}#}
{#                    {% if language.code != code %}#}
{#                    <button class="btn-link btn-block language-select dropdown-item" type="button" name="{{ language.code }}">{{ language.name|slice(0, 2) }}</button>#}
{#                    {% endif %}#}
{#                {% endfor %}#}

{#            </div>#}
{#        </div>#}
{#        <input type="hidden" name="code" value="" />#}
{#        <input type="hidden" name="redirect" value="{{ redirect }}" />#}
{#        </form>#}
{% endif %}