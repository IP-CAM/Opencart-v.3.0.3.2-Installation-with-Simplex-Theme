{{ header }}
<main>
    <div class="container-fluid">
        <ul class="breadcrumb">
            {% for breadcrumb in breadcrumbs %}
                {% if loop.first %}
                    <li><a href="{{ breadcrumb.href }}"><img alt="Home"
                                                             src="catalog/view/theme/simplex/assets/img/home.svg"></a>
                    </li>
                {% elseif loop.last %}
                    <li>{{ breadcrumb.text }}</li>
                {% endif %}
            {% endfor %}
        </ul>
        <div class="page-title">
            <h1>{{ heading_title }}</h1>
        </div>
    </div>
    <div class="container-fluid">
        <div id="account">

            <a id="add" href="{{ add }}" >{{ button_new_address }}</a>
            <br>
            <div style="overflow: auto">
                <table style="min-width: 620px">
                    <thead>
                    <tr>
                        <th>{{ text_address }}</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for result in addresses %}
                        <tr>
                            <td class="text-left">{{ result.address }}</td>
                            <td class="text-right"><a href="{{ result.update }}"
                                                      class="btn btn-info">{{ button_edit }}</a> &nbsp; <a
                                        href="{{ result.delete }}" class="btn btn-danger">{{ button_delete }}</a></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <a class="back" href="{{ back }}">{#Înapoi la Cont Personal#}{{ button_back }}</a>
        </div>
    </div>
</main>
{{ content_bottom }}
{{ footer }}