{#<h3>{{ heading_title }}</h3>
<div class="row">
  {% for information in informations %}
  <div class="information-layout col-lg-3 col-md-3 col-sm-6 col-xs-12">
    <div class="information-thumb transition thumbnail">
      {% if information.thumb %}
      <div class="image"><a href="{{ information.href }}"><img src="{{ information.thumb }}" alt="{{ information.title }}" title="{{ information.title }}" class="img-responsive" /></a></div>
      {% endif %}
      <div class="caption">
        <h4><a href="{{ information.href }}">{{ information.title }}</a></h4>
        {% if author or date or review %}
        <ul class="list-inline text-muted small">
          {% if date %}
          <li>{{ information.date_added }}</li>
          {% endif %}
          {% if author %}
          <li>{{ information.author }}</li>
          {% endif %}
          {% if review %}
          <li>{{ information.reviews }}</li>
          {% endif %}
          {% if rating %}
          <li class="rating">
          {% for i in 1..5 %}
          {% if information.rating < i %}
          <span class="fa fa-stack"><i class="fa fa-star-o fa-stack-2x"></i></span>
          {% else %}
          <span class="fa fa-stack"><i class="fa fa-star fa-stack-2x"></i><i class="fa fa-star-o fa-stack-2x"></i></span>
          {% endif %}
          {% endfor %}
          </li>
          {% endif %}
        </ul>
        {% endif %}
        <p>{{ information.description }}</p>
        <a href="{{ information.href }}" type="button" class="btn btn-primary" title="{{ text_read }}">{{ text_read }}</a>
      </div>
    </div>
  </div>
  {% endfor %}
</div>#}
<div id="news">
  <div class="container-fluid">
    <div class="block-title">
      <h2>{{ heading_title }}</h2>
      <a class="total-link" href="{{ href_category }}">Toate noutăţi</a>
    </div>
    <div class="news">
      {% for information in informations %}
        <div class="card">
          {% if information.thumb %}
            <a href="{{ information.href }}"><img alt="New 1" class="card-img-top" src="{{ information.thumb }}"></a>
          {% endif %}
          <div class="card-body">
            <div class="card-text">
              <a href="{{ information.href }}">{{ information.title }}</a>
            </div>
            <span class="card-date">{{ information.date_added }}</span>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

